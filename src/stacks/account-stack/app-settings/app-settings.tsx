import React from 'react';
import { StyleSheet, View } from 'react-native';

import { AppTheme } from '@/theme';

export const AppSettingsScreen = () => {
  return (
    <>
      <View></View>
    </>
  );
};

const makeStyles = ({ spacing, fontSize }: AppTheme) => StyleSheet.create({});
